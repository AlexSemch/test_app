<li>
  <%= gravatar_for user, size: 52 %>
  <%= link_to user.nik, user %>
  <% if current_user.admin? && !current_user?(user) %>
     | <%= link_to t(:delete), user, method: :delete,
                                  data: { confirm: t(:Y_sure) } %>
  <% end %>
</li>

