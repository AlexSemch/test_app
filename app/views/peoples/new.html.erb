<% provide(:title, 'Edit') %>
<h1>Edit</h1>

<div class="row">
  <div class="span6 offset3">
    <%= form_for(@people) do |f| %>
        <%= render 'fields', f: f %>
        <%= f.submit "Create", class: "btn btn-large btn-primary" %>
    <% end %>
    </div>
</div>
