<%= form_for(@danswer, remote: true) do |f| %>
  <% if @danswer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@danswer.errors.count, "error") %> prohibited this danswer from being saved:</h2>

      <ul>
      <% @danswer.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


    <table border="0px" style="width: 100%">
      <tr>
        <th><%= t(:is_true) %></th>
        <th><%= t(:answer_text) %></th>
      </tr>
      <tr>
        <td><%= f.check_box :ans_is_true %></td>
        <td><%= f.cktext_area :answer_text, :ckeditor => {:language => "uk", :autoParagraph => false, :height => 70 , :toolbarGroups => [
                { name: 'clipboard',   groups: [ 'clipboard', 'undo' ] },
                { name: 'basicstyles', groups: [ 'basicstyles', 'cleanup' ] },
                { name: 'styles' },
                { name: 'colors' }
        ]} %></td>
      </tr>

    </table>
  <div class="actions" style="text-align: center">
    <%= f.submit t(:save),  options={ :class => "btn btn-inverse"} %>
  </div>
<% end %>
